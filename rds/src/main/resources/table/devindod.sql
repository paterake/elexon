CREATE TABLE bmrs.devindod (
header_record_type    VARCHAR(255),
header_file_type      VARCHAR(255),
record_type           VARCHAR(255),
settlement_day        DATE,
volume                int,
d_energy_vol_norm_ref int,
d_energy_vol_low_ref  int,
d_energy_vol_high_ref int,
active_flag           VARCHAR(255)
);