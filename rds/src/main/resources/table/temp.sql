CREATE TABLE bmrs.temp (
header_record_type VARCHAR(255),
header_file_type   VARCHAR(255),
record_type        VARCHAR(255),
spot_Time          DATE,
temp_out_turn      DECIMAL,
normal_temp        DECIMAL,
low_temp           DECIMAL,
high_temp          DECIMAL,
active_flag        VARCHAR(255)
);